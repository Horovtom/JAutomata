package cz.cvut.fel.horovtom.logic.reducers;

public class ENFAReducer {

    public ENFAReducer() {
        //TODO: IMPLEMENT

        /*
          TreeSet<Integer> current = new TreeSet<>();
        for (int initialState : this.initialStates) {
            int[] epsilonClosure = this.getEpsilonClosure(initialState);
            for (int i1 : epsilonClosure) {
                current.add(i1);
            }
        }

        ArrayList<String> states = new ArrayList<>();
        states.add(this)

         */
    }
}
